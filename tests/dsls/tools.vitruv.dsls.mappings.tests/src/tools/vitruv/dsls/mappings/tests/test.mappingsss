import "edu.kit.ipd.sdq.metamodels.addresses" as ad
import "edu.kit.ipd.sdq.metamodels.recipients" as re
    	
mappings: test for Addresses and Recipients
    	
mapping AdRootXReRoot {
   map (ad::Addresses addressRoot)
   and (re::Recipients recipientsRoot)
   
   left reaction IntegrationTest {
		after element ad::Addresses created
		call testIntegrationRoutine(affectedEObject)
   }
   
	routine testIntegrationRoutine(ad::Addresses affectedEObject) {
		action {
			val f = create re::Recipients
		//	add correspondence between f and affectedEObject 
		}
	}
	
	
}


